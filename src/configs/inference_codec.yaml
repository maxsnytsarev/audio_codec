defaults:
  - model: soundstream
  - writer: cometml
  - metrics: audio
  - datasets: inference_data
  - dataloader: librispeech
  - transforms: librispeech
  - _self_

inferencer:
  google_link: "1M_wyp9oSxldQ49ezXeErBeu-F0fJ9fa0"
  from_pretrained: "model_weights/model_weights.pth"
  save_path: "inference_output"
  device_tensors: ["data_object"] # which tensors should be on device (ex. GPU)
  device: auto # device name or "auto"
  seed: 1
